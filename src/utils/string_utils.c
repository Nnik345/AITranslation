#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include "string_utils.h"

char *StrDup(const char *s) {
    if (!s) return NULL;
    
    return NULL;
}

char *StrTrim(char *s) {
    if (!s) return NULL;
    
    return s;
}
